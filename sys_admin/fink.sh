# Fink setup
[[ -r /sw/bin/init.sh ]] && source /sw/bin/init.sh
# should let MacTeX's ghostscript in /usr/local/bin rule?

# finkudprep notification
#[[ -r /etc/finkudprep-motd ]] && cat /etc/finkudprep-motd
